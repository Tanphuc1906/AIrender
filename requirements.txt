# Core
fastapi==0.111.0
uvicorn[standard]==0.29.0
python-multipart==0.0.9
pydantic==2.7.0
httpx>=0.27.0

# AI / Image generation
torch>=2.2.0
torchvision>=0.17.0
diffusers>=0.27.0
transformers>=4.40.0
accelerate>=0.30.0
safetensors>=0.4.3

# Image processing
Pillow>=10.3.0
numpy>=1.26.0

# Optional: xformers for faster GPU inference (install separately if needed)
# xformers>=0.0.26

# Optional: ONNX runtime support
# onnxruntime-gpu>=1.18.0
